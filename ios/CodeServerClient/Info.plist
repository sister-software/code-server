<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDisplayName</key>
	<string>Code</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<!-- Adaptive splash: a single full-screen background color that follows the
	     system appearance (white in light, black in dark). LaunchBackground is a
	     color set in Assets.xcassets. -->
	<key>UILaunchScreen</key>
	<dict>
		<key>UIColorName</key>
		<string>LaunchBackground</string>
	</dict>
	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>arm64</string>
	</array>
	<!-- Dev tool talking to a self-hosted lab over Tailscale/LAN: allow http and
	     self-signed https. Tighten this if you ever distribute beyond yourself. -->
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsArbitraryLoads</key>
		<true/>
	</dict>
	<!-- WKWebView only enables service workers for app-bound domains; the local
	     workbench needs one for VS Code webviews (markdown preview etc.). Only
	     the local web view opts in via limitsNavigationsToAppBoundDomains —
	     remote views stay unrestricted so SSO redirects keep working. -->
	<!-- App-bound mode (per-web-view, local workbench only) unlocks service
	     workers but restricts navigation to these domains. localhost is the
	     workbench origin; the rest are OAuth providers that open in a popup
	     (GitHub sign-in, Settings Sync). Max 10 entries. -->
	<key>WKAppBoundDomains</key>
	<array>
		<string>localhost</string>
		<string>github.com</string>
		<string>vscode.dev</string>
		<string>login.microsoftonline.com</string>
		<string>login.live.com</string>
		<string>microsoft.com</string>
	</array>
	<!-- SSH remoting connects to LAN addresses (e.g. 192.168.x.x); iOS blocks
	     local-network sockets without this and shows a permission prompt. -->
	<key>NSLocalNetworkUsageDescription</key>
	<string>Code connects to your own development server over SSH on your local network.</string>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
		<false/>
		<key>UISceneConfigurations</key>
		<dict>
			<key>UIWindowSceneSessionRoleApplication</key>
			<array>
				<dict>
					<key>UISceneConfigurationName</key>
					<string>Default Configuration</string>
					<key>UISceneDelegateClassName</key>
					<string>$(PRODUCT_MODULE_NAME).SceneDelegate</string>
				</dict>
			</array>
		</dict>
	</dict>
</dict>
</plist>
